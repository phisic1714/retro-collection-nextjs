services:
  nextjs:
    image: node:alpine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules # ให้ container จัดการ node_modules เอง
    ports:
      - "5000:3000"
    command: sh -c "npm install && npm run dev"
    environment:
      NODE_ENV: development
